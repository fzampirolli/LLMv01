print("Digite o tamanho do vetor: ")
int tam = int(input())
Diagonal(m, vetor)
Imprimir(m, vetor)
Media(m, media)
def Diagonal(int m[][], int vetor[]):
    int LIN = 0, COL = 0, l, c
    print("Digite o número de linhas: ")
    LIN = int(input())
    print("Digite o número de colunas: ")
    COL = int(input())
    for 0 in range(0, LIN):
        for 0 in range(0, COL):
            print("Digite o valor da linha " + (l) + " e da coluna " + (c))
    int tam = int(input())
    for 0 in range(0, LIN):
        for 0 in range(0, COL):
            if l == c:
def Imprimir(int m[][], int vetor[]):
    int l = 0, c = 0
    int tam = int(input())
    print(m[l][c])
def Media(int m[][], int media):
    int LIN = 0, COL = 0, l, c, soma = 0
    for 0 in range(0, LIN):
        for 0 in range(0, COL):
        if soma % 5 == 0:
            media = soma / (m.length)
    return media