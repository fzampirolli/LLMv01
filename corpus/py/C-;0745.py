GeraMat(matriz,150,4)
for i=0 in range(0, 150):
    for j=0 in range(0, 4):
        print(" "+matriz[i][j])
    print(" ")
GeraMedia(matriz,150,4)
def GeraMat(char Mat[][], int nlinhas, int ncolunas):
    conceitos = ['A','B','C','D','F']
    int i,j,valor
    for i=0 in range(0, nlinhas):
        for j=0 in range(0, ncolunas):
            valor=(int)(Math.random()*5)
def GeraMedia(char M[][], int nlinhas, int ncolunas):
    for i=0 in range(0, nlinhas):
        for j=0 in range(0, ncolunas):
            if "A".equals(M[i][j]:
            else:
                if "B".equals(M[i][j]:
                else:
                    if "C".equals(M[i][j]:
                    else:
                        if "D".equals(M[i][j]:
                        else:
                            if "F".equals(M[i][j]:
            for i=0 in range(0, 150):
                for j=0 in range(0, 4):
                    print(" "+ma[i][j])
                print(" ")
            for j=0 in range(0, 150):
        for i=0 in range(0, 150):
            print(" "+v[i])