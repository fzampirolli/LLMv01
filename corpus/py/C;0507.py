String ans = "a"
int setor, numfila, numcad
int vend = 0
for 0 in range(0, set1.length):
    for 0 in range(0, set1.length):
for 0 in range(0, set2.length):
    for 0 in range(0, set2.length):
while !"c".equals(ans:
    {
    print("\n Digite a opção desejada: ")
    print("a - Vender ingresso")
    print("b - Exibir o total de ingressos vendidos")
    print("c - Encerrar o programa")
    ans = scan.nextLine()
    if "a".equals(ans:
        print("Digite o setor: ")
        setor = int(input())
        print("Digite o número da fila: ")
        numfila = int(input())
        print("Digite o número da cadeira: ")
        numcad = int(input())
        if setor == 1:
            else:
                {
                print("cadeira já vendida")
        if setor == 2:
            else:
                {
                print("cadeira já vendida")
    if "b".equals(ans:
        for 0 in range(0, set1.length):
            for 0 in range(0, set1.length):
                vend ++
        for 0 in range(0, set2.length):
            for 0 in range(0, set2.length):
                vend ++
        print("Total de ingressos vendidos: "+ vend)